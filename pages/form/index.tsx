import type { NextPage } from 'next';
import WebImageUploadForm from '../../features/webImagesUploadForm';

const FormPage: NextPage = () => {
  return <WebImageUploadForm />;
};

export default FormPage;
